---
title: "ITablePagerHost"
description: "Non-generic view of a Table<T> so the non-generic TablePager can drive paging without knowing the row type."
---

Non-generic view of a `Table<T>` so the non-generic
`TablePager` can drive paging without knowing the row type.
