---
title: "MaskShape"
description: "Shape applied by the daisyUI mask-* utility on a Mask component."
---

Shape applied by the daisyUI `mask-*` utility on a `Mask` component.

## Fields

### `Circle`

Circle clip (`mask-circle`).

### `Diamond`

Diamond clip (`mask-diamond`).

### `Heart`

Heart clip (`mask-heart`).

### `Hexagon`

Hexagon clip (`mask-hexagon`).

### `Pentagon`

Pentagon clip (`mask-pentagon`).

### `Squircle`

Squircle clip (`mask-squircle`).

### `Star`

Star clip (`mask-star`).

### `Star2`

Six-point star clip (`mask-star-2`).

### `Triangle`

Triangle clip (`mask-triangle`).
