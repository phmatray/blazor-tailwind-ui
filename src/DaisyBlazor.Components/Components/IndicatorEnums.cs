namespace DaisyBlazor;

/// <summary>Horizontal axis position of the indicator item (maps to indicator-start / indicator-center / indicator-end).</summary>
public enum IndicatorHorizontal
{
    Start,
    Center,
    End
}

/// <summary>Vertical axis position of the indicator item (maps to indicator-top / indicator-middle / indicator-bottom).</summary>
public enum IndicatorVertical
{
    Top,
    Middle,
    Bottom
}
