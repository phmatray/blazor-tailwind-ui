namespace DaisyBlazor;

/// <summary>Controls which side of the chat row the bubble appears on.</summary>
public enum ChatPlacement
{
    /// <summary>Bubble is aligned to the left (.chat-start).</summary>
    Start,

    /// <summary>Bubble is aligned to the right (.chat-end).</summary>
    End
}
