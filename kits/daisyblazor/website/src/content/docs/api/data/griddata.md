---
title: "GridData<T>"
description: "Result returned by a server-data loader: the rows for the requested page and the total number of rows across all pages."
---

Result returned by a server-data loader: the rows for the requested page and the
total number of rows across all pages. Member names mirror MudBlazor's `GridData<T>`.

## Type parameters

- `T` — Row item type.

## Properties

### `Items`

Rows for the requested page.

### `TotalItems`

Total number of rows across all pages.
