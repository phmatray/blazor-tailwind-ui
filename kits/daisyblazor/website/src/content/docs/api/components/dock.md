---
title: "Dock"
description: "API reference for Dock."
---

## Properties

### `ChildContent`

Items to render inside the dock (use `DockItem` components).

### `Size`

Controls the dock height via daisyUI size modifiers (dock-xs … dock-xl). Null means no modifier (default daisyUI size).
