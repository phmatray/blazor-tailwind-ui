---
title: "Steps"
description: "API reference for Steps."
---

## Properties

### `ChildContent`

Child `Step` elements to render inside the steps list.

### `Vertical`

When true, renders the steps vertically (adds the `steps-vertical` class).
When false (default) the steps are rendered horizontally (`steps-horizontal`).
